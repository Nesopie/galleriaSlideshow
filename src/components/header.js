const Header = (props) => {
    return (
        <header>
            <div>
                <div className="title">
                    galleria.
                </div>
                <div className="startSlideshow">
                    START SLIDESHOW
                </div>
            </div>
        </header>
    );
}

export default Header;
