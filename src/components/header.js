const Header = (props) => {
    return (
        <header>
            <div>
                <div className="title">
                    galleria.
                </div>
                <a href="/starry-night" className="startSlideshow">
                    START SLIDESHOW
                </a>
            </div>
        </header>
    );
}

export default Header;
