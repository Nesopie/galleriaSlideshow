const Header = (props) => {
    return (
        <header>
            <div>
                <div className="title">
                    <a href="/">galleria.</a>
                </div>
                <a href="#/starry-night" className="startSlideshow">
                    START SLIDESHOW
                </a>
            </div>
        </header>
    );
}

export default Header;
